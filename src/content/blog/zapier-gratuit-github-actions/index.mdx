---
title: "Comment j’automatise gratuitement mes tâches avec GitHub Actions (sans Zapier)"
description: Découvrez comment automatiser vos tâches monotones gratuitement avec GitHub Actions. Un guide complet pour remplacer Zapier et économiser des centaines de dollars par mois.
date: 2025-06-21
tags: []
image: ''
authors: ['etiennedo']
---

## TLDR

- Remplace Zapier par des cron jobs avec des GitHub Actions gratuitement

## C'est quoi l'automatisation de tâches

Automatiser ses tâches, c'est juste de laisser un ordi faire ta job monotone. Ça te permet de te concentrer sur des projets plus importants que de l'entrée de donnée ou des calculs de dates simples.

### Exemples de tâches à automatiser

- La génération de centaines de codes promos et son import dans Shopify
- Le calcul d'une date de livraison prévue en comptant un temps de production, puis la génération du bordereau de livraison.
- La vérification de tâches faites par des employés et les notifier automatiquement par Teams le résultat de la vérification.

## C'est quoi Zapier

[Zapier](https://zapier.com/) est une plateforme no-code permettant d'automatiser des tâches. Zapier se connecte à des centaines d'applications et gère l'authentification et les transferts de données. Son modèle d'affaire est simple, il t'offre gratuitement le paramétrage dans un outil sans code et te charge à chaque tâche qu'il effectue.

### Quand Zapier a sa place

Dans une petite équipe qui possède un employé à l'aise avec la technologie et souhaite s'éviter un salaire supplémentaire, c'est évident que Zapier est une belle option. Également, quand l'entreprise n'a pas beaucoup de complexité dans ses tâches, puisqu'il faut utiliser les actions disponibles dans Zapier si on veut éviter de coder.

### Pourquoi j'ai arrêté d'utiliser Zapier

Pour l'utilisation que j'en faisais, Zapier coûte trop cher pour chaque automatisation et ne propose pas assez de personnalisation.

### Tâches limitées

Les tâches étant créées d'avance par Zapier, on n'a pas toujours la personnalisation nécessaire pour automatiser 100% d'un processus. À ce moment là, on doit passer par une option dans Zapier qui permet des appels aux API des applications. Le hic, c'est que ces appels demandent de coder nos requêtes. Tant qu'à coder la demande, on peut explorer comment coder la connexion également et s'éviter la taxe de Zapier sur chaque tâche, puisque la puissance demandée par la requête est loin de coûter ce que Zapier propose. 

### Prix exorbitant

J'ai principalement arrêté Zapier à cause du [prix](https://zapier.com/pricing). Il monte excessivement vite si on veut automatiser des tâches avec plusieurs étapes ou avec de nombreuses activations par jour. Voici un tableau récapitulatif de l'augmentation de prix pour le tier Professionnel (le moins cher de Zapier, avec les fonctionnalités de base).

| Automatisations (tasks) par mois | Prix mensuel (CAD) |
|----------------------------------|--------------------|
| 100                              | $0                 |
| 750                              | $41.67             |
| 1 500                            | $81.28             |
| 5 000                            | $185.48            |
| 10 000                           | $268.85            |
| 100 000                          | $1 019.12          |

Pour donner un référant, une tâche équivaut à une action dans une automatisation. Si on veut, pour chaque commande d'un e-commerce, générer un bordereau de commande et l'imprimer, on aurant 3 tâches:
1. Aller chercher les données de la commande
2. Générer le bordereau avec les données de la commande
3. Lancer l'impression

Pour une commande par jour, ça se fait bien, mais dès qu'on arrive à par exemple 100 commandes par jour, on parle de plus de 250$ par mois. Ça c'est pour une seule automatisation!

## Comment GitHub peut automatiser le travail gratuitement

GitHub permet de faire presque la même chose que Zapier, avec 6h de processus pour 20 tâches concurrentes gratuitement chaque jour. C'est à dire qu'on peut faire rouler les serveurs de GitHub pour nos actions pendant 6h par tâche, pour 20 tâches en même temps, c'est immense pour des petites automatisation! Gratuitement!
On utilise principalement deux technologies pour faire ces automatisations.

- [GitHub Actions](https://docs.github.com/fr/actions/about-github-actions)
- des [cron jobs](https://www.hostinger.com/fr/tutoriels/cron-job)
- [GitHub secrets](https://docs.github.com/fr/actions/security-for-github-actions/security-guides/using-secrets-in-github-actions)
- les [API](https://fr.wikipedia.org/wiki/Interface_de_programmation) des applications qu'on utilise

### GitHub Actions c'est quoi?

Les GitHub Actions permettent d'exécuter des scripts gratuitement sur les serveurs de GitHub.
Habituellement, les Actions sont utilisées pour formatter du code éviter les bugs dans du code.

Avec l'utilisation d'une cron job, on peut utiliser cette technologie pour d'autres types de scripts.

### Qu'est-ce qu'une cron job

En gros, une cron job c'est une façon de céduler le lancement d'une tâche sur un ordi.

[Comment utiliser un cron job avec GitHub Actions?](/astro-blog/blog/cron-job-github-actions)

### Les GitHub secrets c'est quoi?

Les GitHub Secrets permettent de sauvegarder du texte de façon sécurisée. On peut ensuite le référer dans le code avec un alias comme ```${MON_SECRET}```. Ça évite d'entrer des mots de passes ou clés d'API (expliqués juste en dessous) en texte dans notre code.

### Une API c'est quoi?

Une API, ça nous permet d'utiliser un script pour voir ou manipuler les données d'une application.
On va utiliser une **Clé d'API** en guise de mot de passe pour accéder aux données de l'application.

### Tutoriel pour automatiser des tâches avec GitHub Actions

### Modèle de fichier YAML pour automatiser ses tâches avec un script Node.js

Exemple de fichier YAML (workflows/automatisation.yaml)

```yaml
name: Automatisation quotidienne en Node.js

on:
  schedule:
    - cron: "0 0 * * *" # Tous les jours à minuit

jobs:
  run-node-script:
    runs-on: ubuntu-latest
    steps:
      - name: Récupérer le dépôt
        uses: actions/checkout@v3

      - name: Installer Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Installer les dépendances
        run: |
          npm install

      - name: Exécuter le script principal
        env:
          API_KEY: ${{ secrets.API_KEY }} # Tu peux passer des variables d'environnement ici
        run: |
          node mon_script.js
```

Exemple de fichier `mon_script.js` qui récupère des données via une API et les affiche dans la console.

```js
// mon_script.js

const axios = require('axios');

console.log("🚀 Démarrage du script...");

async function fetchData() {
  try {
    const response = await axios.get('https://jsonplaceholder.typicode.com/todos/1');
    
    // Affichage des données récupérées
    console.log("✅ Données récupérées :");
    console.log(response.data); // C'est ici qu'on affiche les données

  } catch (error) {
    console.error("❌ Erreur lors de la requête :", error.message);
  }
}

fetchData();
```

🔎 Ce que tu verras dans les logs de GitHub Actions

```
🚀 Démarrage du script...
✅ Données récupérées :
{ userId: 1, id: 1, title: 'delectus aut autem', completed: false }
```

## 🧰 Alternatives à Zapier

| Plateforme     | Type              | Gratuit ?       | Hébergement requis | Langage/code nécessaire | Notes |
|----------------|-------------------|------------------|---------------------|--------------------------|-------|
| **n8n**        | No-code / Low-code | ✅ Oui           | ✅ Oui (VPS/Docker)  | ❌ Non                   | Interface graphique, open-source, supporte +300 apps |
| **Make (Integromat)** | No-code         | ⚠️ Partiellement | ❌ Non               | ❌ Non                   | Très similaire à Zapier, moins d’intégrations |
| **Pipedream**  | Low-code / API     | ✅ Oui           | ❌ Non               | ✅ Oui (JS/Node.js)      | Idéal pour des workflows techniques |
| **GitHub Actions + Cron Jobs** | Code-first | ✅ Oui           | ❌ Non               | ✅ Oui                  | Contrôle total, gratuit avec limites |
| **IFTTT**      | No-code            | ✅ Oui           | ❌ Non               | ❌ Non                   | Simple mais très limité |


## FAQ

### Est-ce que GitHub Actions est vraiment gratuit ?
Oui, jusqu’à **6 heures par workflow** et jusqu'à **20 workflows concurrents** pour les repos publics.

### Faut-il savoir coder ?
Un peu oui, mais tu peux copier-coller des modèles existants.

### Comment tester mes workflows ?
Depuis l’interface GitHub, tu peux voir les logs en temps réel.

### Peut-on envoyer des emails avec GitHub Actions ?
Absolument, via SendGrid, SMTP ou d'autres services.


### Articles recommandés